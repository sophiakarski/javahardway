// If Statements with Stringsimport java.util.Scanner;public class SecretWord {	public static void main( String[] args ) {		Scanner keyboard = new Scanner(System.in);		String secret = "please", woo = "password", guess;				System.out.print( "What's the secret word? " );		guess = keyboard.next();				if ( guess.equals(secret) || guess.equals(woo) ) {			System.out.println( "That's correct!" );			}		else {			System.out.println( "No, the secret word isn't \"" + guess + "\"." );		}				// NEVER SHOWS!		if ( guess == secret ) {			System.out.println( "(This will never ever show, no matter what.)" );		}			}}// Line 8: defined (declared + initialised) value to String 'secret'// !!!!!!! -> .equals() method/* Line 21: will NEVER be true ( '==' only compares shallow values of variables... )	SHALLOW values of 2 Strings are only equal when they refer to SAME MEMORY ALLOCATION 		What does work: '.equals()' method : compares DEEP values of variables instead */