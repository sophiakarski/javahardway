// Variables only hold Valuesimport java.util.Scanner;public class Sequencing {	public static void main( String[] args ) {	// THIS CODE IS BROKEN UNTIL YOU FIX IT		Scanner keyboard = new Scanner(System.in);	double price, salesTax, total;	// 	salesTax = price * 0.0825;// 	total = price + salesTax;		System.out.print( "How much is the purchase price? " );	price = keyboard.nextDouble();		salesTax = price * 0.0825;	total = price + salesTax;		System.out.println( "Item price:\t" + price );	System.out.println( "Sales tax:\t" + salesTax );	System.out.println( "Total cost:\t" + total );	}}/* LOGICAL ERROR: not syntax error (the compiler cares about), not a runtime error that happens with wrong variable type.	LOGICAL ERROR = flaw with how flow of instructions are designed -> output is not what trying to accomplish *//* Java (& most other): VARIABLES CANNOT HOLD FORMULAS, only VALUES	e.g. Line 12...*/	// Line 10: DECLARE + INITIALISE variable (= DEFINING)// VARIABLES hold VALUES, NOT FORMULAS// Programmes = sequence of instructions that computer executes in order